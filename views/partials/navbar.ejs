<nav class="navbar navbar-expand-lg navbar-light">
    <div class="container-fluid">
        <img class="nav-logo" src="/img/courseifyLogo.png" width="150" height="50" class="d-inline-block align-top"
            alt="">
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup"
            aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
            <div class="navbar-nav">
                <a class="nav-link" href="/courses/">Courses</a>
                <a class="nav-link" href="/courses/new">Create a Course</a>
            </div>
            <div class="navbar-nav ms-auto">
                <% if(!currentUser) { %>
                    <a class="nav-link" href="/register">Login/Register</a>
                    <% } else { %>
                        <a class="nav-link" href="/logout">Logout</a>
                        <a class="nav-link" href="/users/<%= currentUser._id %> ">My profile</a>
                        <% } %>
            </div>
        </div>
    </div>
</nav>